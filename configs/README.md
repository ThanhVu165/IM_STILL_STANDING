# Configuration

Configuration files for the AIC 2026 multimodal video retrieval system.

Configuration is kept separate from implementation so model, preprocessing, retrieval, database, and runtime parameters remain configurable.
